require "config"
require "recfile"

require "manager"

require "config"
require "ui/frame"

require "play"
require "score"
require "input"
require "pattern"

ARGV.each{|fname|
  obj = RecFile.open(fname)
  p obj
  obj.seginfo.each_index{|i|
    obj.view_segment(i)
  }
}
